# Environment Variables
# Copy this file to .env.local and fill in your values

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# OCR Space API
OCRSPACE_API_KEY=your_ocrspace_api_key

# Google Gemini API
GEMINI_API_KEY=your_gemini_api_key

# Next.js Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret

# Upstash Rate Limiting
UPSTASH_REDIS_REST_URL=your_upstash_redis_url
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_token

# Debug Configuration
NEXT_PUBLIC_DEBUG=true
NEXT_PUBLIC_LOG_LEVEL=DEBUG

# ===================================================================
# CLERK AUTHENTICATION CONFIGURATION
# ===================================================================
# Get these from https://dashboard.clerk.com → Your App → API Keys

# Core Clerk API Keys
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_YOUR_PUBLISHABLE_KEY_HERE
CLERK_SECRET_KEY=sk_test_YOUR_SECRET_KEY_HERE

# Clerk Authentication Flow URLs
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/onboarding

# Clerk Webhook Configuration
# Get this from Clerk Dashboard → Your App → Webhooks → Add Endpoint
# The endpoint URL should be: https://your-domain.com/api/webhooks/clerk
CLERK_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# ===================================================================
# CLERK SETUP INSTRUCTIONS
# ===================================================================
# 1. Getting API Keys:
#    - Go to https://dashboard.clerk.com
#    - Select your application or create a new one
#    - Navigate to "API Keys" in the sidebar
#    - Copy the Publishable Key and Secret Key
#
# 2. Setting up Webhooks:
#    - In Clerk Dashboard, go to "Webhooks" in the sidebar
#    - Click "Add Endpoint"
#    - Enter: https://your-domain.com/api/webhooks/clerk
#    - Select events: user.created, user.updated, user.deleted
#    - Copy the "Signing Secret" (starts with whsec_)
#
# 3. Supabase Integration:
#    - In Clerk Dashboard, go to "JWT Templates"
#    - Create a template named "supabase"
#    - Template JSON: {"aud": "authenticated", "role": "authenticated", "allowed_roles": ["authenticated"]}
#    - In Supabase Settings → API, set JWT Secret to your Clerk secret key
#
# For detailed instructions, see .env.clerk.template